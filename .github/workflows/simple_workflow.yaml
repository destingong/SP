name: simple-workflow

on: 
    workflow_dispatch:

jobs:
    run-hello-world:
      runs-on: ubuntu-latest
      steps:
          - name: Run hello world
            run: 
              python hello_world.py
