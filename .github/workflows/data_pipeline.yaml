name: data-pipeline-workflow

on: 
    push:

jobs:
    run-data-pipeline:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repo content
              uses: actions/checkout@v4
            - name: Run data pipeline
              run: python code/save_hist_news.py --start "2025-01-01" --end "2025-01-31"
